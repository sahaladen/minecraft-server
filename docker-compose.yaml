version: '3.8'
services:
  minecraft:
    image: sapmachine:latest
    ports:
      - '25565:25565'
    volumes:
      - ./:/app
    container_name: minecraft
    command: sh -c 'cd /app; java -Xmx10G -Xms5G -jar server.jar nogui'
    stdin_open: true
    tty: true